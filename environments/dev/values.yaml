# dev environment for news-service
# Cluster: kind-dev (set up by tf-platform)
# Chart: oci://ghcr.io/tf-systems/charts/news-service

image:
  registry: ghcr.io/tf-systems
  tag: "0.1.0"
  pullPolicy: IfNotPresent
httpserver:
  service:
    type: NodePort
    nodePort: 31000 # matches kind-dev port-mapping (tf-platform clusters/kind-dev/config.yaml)
