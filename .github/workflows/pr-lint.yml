name: pr-lint

on:
  pull_request:
    types: [opened, edited, synchronize, reopened]

permissions:
  pull-requests: read

jobs:
  pr-title:
    uses: tf-systems/tf-pipeline/.github/workflows/pr-title-lint.yml@v0.0.1
